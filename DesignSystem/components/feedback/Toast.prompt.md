# Toast

Ephemeral confirmation after batch actions (labels saved, dataset synced, fine-tune queued).
```jsx
<Toast tone="success" title="Saved" description="127 clips labelled and queued for training." />
```
