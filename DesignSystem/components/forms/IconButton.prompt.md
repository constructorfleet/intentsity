# IconButton

Square button for a single glyph. Requires `aria-label`.

```jsx
<IconButton aria-label="Play" onClick={play}><PlayIcon/></IconButton>
<IconButton variant="primary" aria-label="Record"><MicIcon/></IconButton>
```
