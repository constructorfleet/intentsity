# Input

Text field with optional prefix/suffix glyphs. Use `invalid` to show error state; pair with a helper text below.

```jsx
<Input placeholder="Wake word phrase" prefix={<SearchIcon/>} />
<Input invalid defaultValue="hey siri" />
```
