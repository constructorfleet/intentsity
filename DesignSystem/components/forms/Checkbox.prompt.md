# Checkbox

For multi-select of labels, filters, batch selection in dataset tables.
```jsx
<Checkbox checked={sel} onChange={setSel} label="Include background noise clips" />
```
Use `indeterminate` for parent rows in a partially-selected group.
