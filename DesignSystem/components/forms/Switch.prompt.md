# Switch

Binary on/off for settings — auto-advance after annotation, keyboard shortcuts on, dark mode.
```jsx
<Switch checked={autoAdvance} onChange={setAutoAdvance} label="Auto-advance after label" />
```
