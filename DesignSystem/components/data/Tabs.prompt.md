# Tabs

Section switcher with optional numeric badges — datasets, models, sessions.
```jsx
<Tabs value={tab} onChange={setTab} tabs={[
  {value:'unlabeled', label:'Unlabeled', count:1284},
  {value:'labeled', label:'Labeled', count:512},
  {value:'flagged', label:'Flagged', count:8},
]} />
```
